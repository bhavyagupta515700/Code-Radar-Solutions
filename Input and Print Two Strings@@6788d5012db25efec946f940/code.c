#include <stdio.h>

int main() {
    char str1[100]; // Adjust size as needed
    char str2[100]; // Adjust size as needed
    scanf("%s", str1);
    scanf("%s", str2);
    printf("You entered: %s and %s", str1, str2);
    return 0;
}
