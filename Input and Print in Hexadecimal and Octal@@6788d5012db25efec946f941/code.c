#include <stdio.h>

int main() {
    int num;
    
    scanf("%d", &num);
    printf("The hexadecimal representation is: %X\n", num);
    printf("The octal representation is: %o\n", num);
    return 0;
}
